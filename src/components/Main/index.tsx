import * as S from './styles'

const Main = ({}) => {
  console.log(process.env.NEXT_PUBLIC_KEY, 'aqui')
 
return (
  <S.Wrapper>
    <S.StyledPanel>
    </S.StyledPanel>
  </S.Wrapper>
  )
}

export default Main
